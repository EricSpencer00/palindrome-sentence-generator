# Proper-name scene seam construction — 2026-09-19

This fresh lane constructs two complete, agreement-safe event clauses online
from the outer character obligation. The left clause uses an ordinary
`Name, the role, verb the object place` frame; the right clause front-loads the
place adjunct (`Place, Name, the role, verb the object`). That deliberate
fronting forces a cross-word seam shift in the mirrored alignment, rather than
an aligned whole-token chain. Names, roles, verbs, objects, and places are
small authored banks, not catalogue text.

The remote run on `hst-bench` generated 512 candidates. Every candidate has a
recorded seam-shift gate, online character trace, rendered text, and independent
normalized two-pointer plus forward/reverse SHA-256 audit in
`runs/proper-name-scene-online-20260919.json`. The longest candidate is 77
letters; exact closures: 0; best online matched prefix: 0. The retained best
render is:

> Nora, the artist, starts the parcel near the gate; Near the gate, Ada, the artist, starts the parcel

This is a constructive diagnostic, not a reader-eligible result. Programmatic
exactness and seam checks do not provide human-readability evidence.
