# Semantic-relay outer-edge repair (2026-09-18)

The parent semantic-relay lane failed immediately after matching the first
character: every left clause began with `the`, while its right consequence
object could not supply the reverse prefix. This targeted repair authored
eight vowel-initial subjects with `an` and grammatical terminal `an arena`
objects, then carried the residual through the complete two-clause grammar.

## Result

Representative output:

> **An archivist marks a ledger at dusk; she maps an arena.**

All eight probes are complete clauses and advance the outer match to two
characters, but none closes exactly. There are 0 exact rows, 0 mechanically
admitted rows, and 0 reader-eligible rows. The best frontier dies at the actor
boundary (`era` versus `archivist`), showing that determiner repair alone is
insufficient.

The next operator must co-design the first subject token and terminal object
suffix, not widen the object bank. All rows retain independent pointer/SHA
audits and the hard reversed-token and hidden-span filters.
