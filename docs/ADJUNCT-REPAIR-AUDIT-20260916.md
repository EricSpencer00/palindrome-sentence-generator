# Adjunct repair audit (2026-09-16)

The weighted synchronous CFG adjunct repair was replayed against its frozen
frame. The base grammar produced 13,392 character-position states and the
adjunct-expanded frame produced 82,512 more (95,904 total); both produced zero
exact closures and zero reader-eligible outputs. The packaged evidence remains
unchanged at `runs/weighted-cfg-sync-dp-20260916.json`.

The bounded frame is therefore not a meaningful exact-candidate source. No
repeated wrapper or catalogue text should be added.

## Distinct next operator

Try **semantic role reassignment with lexical entailment checks**: construct a
small event graph first, then swap agent, patient, and instrument roles across
independently authored events while preserving entailment constraints. Solve
the resulting character equations over lexical realizations, with dependency
order determined by the event graph rather than by an adjunct wrapper. Reject
any repeated content word, catalogue tape, mirrored lexical unit, or incomplete
finite frame before retaining a probe. Any exact survivor still requires an
independent audit and blinded readers; readability must not be inferred from a
language-model score.
