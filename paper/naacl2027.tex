\documentclass[11pt]{article}
\usepackage[review]{acl}
\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{enumitem}
\title{Scene-Conditioned Construction of Long English Letter-Level Palindromes}
\author{Anonymous submission}
\newcommand{\norm}[1]{n(#1)}
\newcommand{\rev}{\operatorname{rev}}

\begin{document}
\maketitle

\begin{abstract}
We study the constructive generation of long English palindromes under a
strict letter-level definition.  A candidate must be an exact palindrome,
originate from an auditable construction rather than borrowed catalogue text,
avoid repeated or self-palindromic units, and survive a blinded human
readability study.  Our central method is a live scene-conditioned
character-obligation constructor: it jointly chooses typed semantic spans,
grammar states, and word boundaries while consuming the opposing character
equation.  It grows intact prose from fresh lexical and discourse plans,
rather than writing a sentence and mirroring it afterward.  Evidence-guided
replay is used only to choose which genuinely new construction topology to try
next; it is not a post-hoc editing objective or a readability judge.  Ten deliberately
orthogonal lanes share an independent two-pointer and hash verifier,
provenance records, and a novelty registry.  The manuscript leads with
rendered construction traces, exact gates, and the path toward blinded reader
evidence.  The contribution is a reproducible route toward genuinely readable
long output, not a speed benchmark, proxy-only result, or negative-result
thesis.
\end{abstract}

\section{Task and acceptance gate}
Let $x$ be a rendered English passage.  We normalize by lowercasing and
retaining ASCII letters only,
\begin{equation}
  \norm{x}=\operatorname{letters}(\operatorname{lower}(x)),
\end{equation}
and require $\norm{x}=\rev(\norm{x})$.  Punctuation and spacing are displayed
for readers but cannot change the equation.  The target is an intact,
multi-clause passage of at least 100 normalized letters; the familiar
38-letter seed is used only as a development benchmark.

The hard gate has four independent parts.  First, a standard-library
two-pointer check and a separately implemented forward/reverse hash check
must agree.  Second, the provenance ledger must show that the text was
constructed by the current method, with no catalogue import, scaffold
relexicalization, repeated multiword unit, word-order reflection, or
self-palindromic word/span.  Third, the rendering must be ordinary prose with
distinct lexical choices, not a list or fragment.  Finally, blinded readers
must judge it readable and coherent.  Programmatic language scores can rank
or diagnose candidates; they never certify the last condition.

\section{A working construction loop}
The constructor maintains a state
$(L,R,d,\sigma_L,\sigma_R,\rho)$, where $L$ and $R$ are the committed left
and right tapes, $d$ is the vector of mirrored character debt, $\sigma$ stores
grammar and agreement state, and $\rho$ is the semantic scene graph.  At each
step it (i) chooses a typed scene slot or boundary, (ii) proposes a short
ordinary word or inflection compatible with $\sigma$ and $\rho$, (iii) updates
the opposing character obligation immediately, and (iv) retains the best
frontier by length and unresolved debt.  A new branch may choose a different
semantic slot, resegment a boundary, change a dependency seam, or alter an
inflection/clitic boundary.  A run is not counted as progress when it merely
resamples the same lane or duplicates a prior signature.

This is a character-constrained generation problem in the spirit of prior
palindrome enumeration and lexically constrained decoding
\citep{papadopoulos2015palindromes,hokamp2017grid,lu2022astar}, but the
character obligation is coupled to a live scene and grammar rather than
applied after a sentence has been written.

The ten lanes are intentionally different views of this same loop:
character-level LM decoding; exact-tape grammatical resegmentation;
dependency-tree seam CSP; agreement-carrying morphology; CFG/Earley character
intersection; a human-authored scene lattice with live equations; semantic
valency and attachment; inflectional and clitic boundary search; a flat,
compositional grammar without nested palindrome spans; and semantic slot
substitution.  Each lane writes its rendered text,
normalized tape, independent checks, source hashes, novelty result, and a
concrete next construction to a JSON artifact.

\section{Evidence-guided construction branching}
\label{sec:dream-rsi}
Dream-RSI treats accumulated discovery history as a replay simulator for
exploration policies \citep{zheng2026dreamrsi}.  We use that idea only as an
orchestration layer for choosing the next genuinely new constructor, not as a
generator, post-hoc editing objective, or automatic readability judge.  Each completed
lane contributes a node containing its rendered surface, independently
recomputed exactness, residual signature, provenance, and next construction
topology.  A deterministic policy frontier is trained on one parity split of
these nodes; the selected policy is evaluated on held-out nodes and then used
to route the next live constructor.  Exactness and reader status are never
replay rewards.

The live operator is masked-span infilling.  A state freezes already matched
outer character assignments, identifies the first unresolved seam, and
reopens the seam-owning typed constituents on both sides.  Reopened spans may
change lexical content, inflection, and word boundaries; all other assignments
remain fixed.  Narrow one-slot and wider constituent policies are compared in
replay before held-out deployment.  The current rounds render complete scene
traces such as ``The careful baker records a blue map near the harbor. A young
gardener carries winter herbs after class.'' (85 letters, 33--35 unresolved
character pairs) and ``At first light, the keeper marks the signal, while the
sailor notes the route, by the inlet.'' (72 letters, exactness false).  These
surfaces are construction evidence, not promoted palindrome examples: each
carries its generator hash, independent pointer/SHA audit, and the next
geometry-changing construction.

The latest typed-relative CFG branch changes the grammar topology before any
surface is rendered: 80 typed relative-clause phrases are intersected with
309,680 variable boundary shifts and live equations, all of which prune before
a complete rendering.  A separate valency-indexed phrase-graph branch renders
540 paired-clause diagnostics (maximum 80 letters) from 180 graph edges and
three boundary shifts.  Its strongest surface duplicates and fragments a
clause, so it is quarantined rather than sent to readers.  These two branches
provide distinct next states---dual relative attachments and an incremental
valency edge trie---instead of repeating a larger sweep.
Between them, the semantic-cadence branch retained four vivid scene
diagnostics to 121 letters after eight seam prunes while carrying held-out
animate/inanimate role alternations.  It also produced no exact closure; its
next state is tense-compatible cadence slots, so its prose is evidence for
the constructor and not a readability claim.

The following three held-out branches keep the same gate while changing the
construction state.  Tense-compatible cadence growth retains four vivid
diagnostics to 121 letters; an incremental valency edge trie retains 15,360
ordinary-clause controls to 53 letters; and a dual-relative antecedent graph
retains 72 complete scenes to 108 letters.  All three have zero exact rows
above the 38-letter anchor, so their surfaces remain quarantined diagnostics.
Their next states are aspect-compatible cadence, synchronized dual tries, and
typed relative agreement in a residual trie, respectively.

The subsequent aspect-compatible cadence, synchronized paired-valency, and
typed dual-relative residual branches likewise retain only quarantined
controls: they produce four diagnostics to 121 letters, 12 paired controls to
119 letters, and 72 scenes to 88 letters, respectively, with no exact row above
the anchor.  Their construction states are explicit next steps rather than
reader evidence.

The voice, asymmetric-tense, and lexical-edge branches keep the same
reader-first gate while changing the carried state: four voice diagnostics to
121 letters, 36 tense-conditioned controls to 137 letters, and 48 lexical-edge
diagnostics to 100 letters.  None has an exact row above the anchor; each is
retained only to drive the next construction.

The modality, residual-buffer, and chained-role branches continue this
construction-first progression: four modal diagnostics reach 121 letters, 36
buffered paired controls reach 137 letters, and 72 alternating-role scenes
reach 155 letters.  None has an exact row above the anchor, so none enters the
reader package; each supplies the state for the next topology.

Polarity-conditioned cadence, synchronous residual consumption, and the
three-edge tense transition continue to supply bounded, inspectable states:
four diagnostics to 121 letters, 36 paired controls to 137 letters, and 192
scenes to 195 letters.  Their exact-above-anchor counts remain zero, so the
surfaces stay outside the reader package while the next constructions are
explicitly recorded.

The evidential, residual-mismatch, and four-edge aspectual branches extend the
same audit trail to 121, 137, and 238 letters respectively.  Their exact
counts above the anchor remain zero; the bounded fallback is explicitly
diagnostic-only, and none of these controls is reader-facing.

The discourse, optional-adjunct, and aspectual-automaton branches continue to
separate construction evidence from reader evidence: their longest diagnostics
reach 121, 148, and 127 letters, respectively, while exact-above-anchor counts
remain zero.  The optional edge and aspect states therefore remain active
search topology, not promoted prose.

The quotation, two-adjunct scope, and dual-aspect branches extend the same
construction state without relaxing the gate: their diagnostics reach 121,
161, and 127 letters, respectively, while exact-above-anchor counts remain
zero.  They remain outside the reader package and supply the next topology
for each lane.

For length scaling, the same constructor can grow a recursive discourse spine,
\(\textsc{Spine}\mathrel{::=}\textsc{Event}\ (\textsc{TypedAdjunct})^*\).
Each adjunct is a complete, semantically attached temporal, locative,
instrumental, or causal event sentence.  The derivation state carries the live
outer-character equation and chooses a fresh typed adjunct or reopens an
existing adjunct boundary at the first unresolved pair.  Across the current
construction rounds this grammar produced intact prose at requested 100,
150, 200, and 300-letter targets (roughly 170--394 letters after
normalization, depending on the live center and adjunct choices); none was
exact.  The constructor now retains a residual-ranked frontier over center,
first-adjunct, and terminal-adjunct choices, with duplicate-adjunct rejection.
This is a scalable construction mechanism and a concrete frontier, not
a length-based admission shortcut: every target still requires the independent
exact gate and, only after survival, the blinded reader gate.

Replay makes the search policy scalable without turning a score into a
readability claim.  A fresh branch can spend its bounded budget on a new
constituent geometry instead of resampling a completed sentence.  Only a novel
exact row with intact prose enters the randomized intact-versus-shuffled reader
study.

\paragraph{Latest Dream-RSI evaluation.}
On the 2026-09-18 snapshot, the registry-corrected round 118 replayed 573
worlds and 21,806 independently audited nodes; the held-out
mismatch-first policy remained at 0.780 and produced zero admissible exact
rows.  A fresh deployment of that policy generated an eight-node branching
tree (not a replay duplicate).  Its intact, provenance-traceable controls
include ``The theater archivist found a torn playbill in a locked drawer,
carried it to the reading table, and marked the missing cast names before the
house lights rose.'' (130 normalized letters, 60 mismatches) and ``The stage
archivist retrieved a torn playbill from a locked drawer, carried it to the
reading table, and marked the missing cast names as the house lights dimmed.''
(132 letters, 62 mismatches).  All eight controls are exactness-false under
the independent two-pointer and forward/reverse SHA-256 audits; none is
reader-certified.  The next reader-facing test is therefore still gated: it
will randomize any future exact survivor with an intact/shuffled pair and
blinded raters, rather than treating replay residuals as readability evidence.

The current live constructor is now evaluated on fresh role-bearing
topologies rather than larger replay sweeps.  An asynchronous three-way join
keeps agent, theme, and attachment residuals separate and retains broad
English controls to 97 letters; possibility-modal and modal-scope branches
likewise preserve intact lexical and agreement states while consuming the
character equation online.  Independent semordnilap-scene and synchronous
clause-growth branches likewise retain intact controls from 71 to 96 letters;
these surfaces remain construction controls until
an exact closure survives the no-shortcuts audit.  Their concrete next
operators---cross-stream synchronization, probabilistic modal scope, and new
typed attachment states---are recorded with immutable artifacts so that every
failed run changes the constructor rather than becoming a negative-result
claim.

The next construction turn changed the search geometry rather than enlarging
that replay.  A grammar-aware seam enumerated 3,456 fresh complete scene
frames around the withheld 38-letter benchmark and found no reflected frame
pair; six typed semordnilap residual repairs also closed zero seams.  The
resulting typed reflected-residual chart then grew two scene frames from
opposite character edges, carrying debt across word boundaries while checking
agreement and transitivity before closure.  It expanded three live states
across three frame pairs and found zero exact rows.  Its fresh controls were
67--68 letters and are reported only as independently audited, human-unreviewed
prose.  These failures select the next constructive operator---a
role-preserving lexical-category transition at a live boundary---rather than
relaxing exactness or promoting a fragment.

We then closed an implementation gap in the denoising hypothesis itself.  A
cached GPT-2 policy was actually queried for 12 complete seam spans, and a
bounded lexical chart independently resegmented each reflected residual.  The
12 resulting surfaces are exact by construction and range from 86 to 120
normalized letters, but none admits a lexical segmentation on the reflected
side, so zero are shortcut-free admissions.  Their model scores are routing
signals only; no row is reader-certified.  The 38-letter user sentence used
to keep the first tape exact is explicitly bootstrap structure rather than a
claimed novel output.  The next constructive repair therefore replaces that
scaffold with two independently model-authored boundary clauses and carries
the residual chart across both seams before any reader study.

The corresponding Dream-RSI replay ingested those saved model worlds as
alternatives rather than as a larger sweep.  Across 21,990 independently
audited nodes it retained 120 exact-but-fragment controls and zero admissible
exact rows; the held-out mismatch-first policy stayed at 0.780.  This is a
policy-selection result, not a readability result, and it sends the next
construction turn to a two-boundary model-authored scene with no bootstrap
sentence in the output.

That two-boundary turn queried the local language model independently for eight
left and eight right complete clauses, then crossed them into 64 fresh worlds.
The seed sentence was absent from every rendered surface.  The best intact
pair was 58 letters and none was exact; the first residual is therefore a
complete-word obligation for the next operator rather than a reason to relax
the gate.

A separate discourse operator tested a polar question followed by a
topicalized answer, with typed word-boundary resegmentation.  It yielded 148
exact diagnostic rows of 42--46 letters.  The longest cross-boundary row was
``Was Noel an era, a gas, an item? Met in a, saga, arena, Leon saw.''  Its
two-pointer and forward/reverse SHA-256 checks agree, but it is rejected by
the strict gate because it contains a proper palindromic island; none of the
148 rows is reader-ready.  The next construction jointly authors a complete
answer clause and rejects every proper palindromic subspan before closure.

The implementation also exposes an explicit totality witness for arbitrary
requested lengths.  This witness is useful for testing the state machine but
is tagged as non-reader output and cannot satisfy the admission gate.  Thus
termination of the software is never confused with success of the linguistic
task.

\section{Current rendered evidence}
Table~\ref{tab:lanes} gives one fresh surface for each lane family.  These are
actual outputs, not templates; lengths count normalized letters.  Exactness
and readability status are shown together so that a high-scoring but invalid
surface cannot become a headline result.

\begin{table*}[t]
\centering
\scriptsize
\begin{tabular}{@{}p{0.20\linewidth}p{0.34\linewidth}p{0.06\linewidth}p{0.05\linewidth}p{0.23\linewidth}@{}}
\toprule
Lane family & Rendered surface & Letters & Exact & Next repair \\
\midrule
LM + tape & the careful teacher guides a young visitor; a patient sailor carries the red lantern & 70 & no & held-out LM boundary proposals \\
Dependency + morphology + CFG & Ava saw radar level civic; civic level radar was Ava & 42 & yes$^\dagger$ & typed boundary resegmentation / fresh frame \\
Scene lattice & At dawn, the patient guide carries a red map to the waiting child. & 52 & no & replace one typed object slot \\
Valency / attachment & By noon, the careful nurse labels the sealed vial for the quiet ward. & 55 & no & preserve valency while changing object/attachment \\
Inflection / clitic & After rain, the young keeper opens the old gate beside the garden. & 53 & no & held-out inflection-boundary repair \\
Flat composition / slot repair & At dawn, the patient guide carries a blue chart to the waiting child. & 55 & no & satisfy the global character obligation \\
\bottomrule
\end{tabular}
\caption{Representative outputs in the 2026-09-16 ledger.  $^\dagger$The
42-letter seam is exact under both independent checks but is rejected because
it repeats self-palindromic words, mirrors word order, and fails the distinct
word gate.  The other rows are fresh ordinary prose and are retained as
repairable frontiers, not as palindrome claims.}
\label{tab:lanes}
\end{table*}

The aggregate also records a 1,922-letter recursive clause-growth diagnostic;
it is retained as a scalability probe, but fails exactness and the repetition
and length gates, so it is not a reader-facing result.  Among the complete
prose frontiers, the CFG/Earley lane reaches 270 letters and the character-LM
lane reaches 227 letters; neither closes exactly.  A tape resegmentation lane
also found the 21-letter catalogue
control ``a man a plan a canal panama''; its exactness is independently
confirmed, but catalogue provenance excludes it.  These controls are useful
for checking the gates, never for inflating the result set.

Three further search-space probes are retained as separate, non-duplicate
construction evidence.  The simultaneous grammar-product beam emits complete
typed scenes such as ``The young keeper carries a blue lantern and rests
quietly beside the garden as evening falls.'' (77 letters); its live
reverse-character ledger and independent two-pointer audit both report
non-exactness, and its next repair seam-relexicalizes the follow-up to consume
the outstanding obligation.  An initial reverse-tape trie
boundary beam produced the exact surface ``no one was at home | em oh ta sa we
no on'' (28 letters; forward/reverse SHA-256 agree), but the right side was
visibly fragmentary.  The tagged-POS, frequency-filtered follow-up correctly
returned no complete clause rather than relabeling a fragment as prose; its
next repair expands attested subject/verb/object senses without lowering the
frequency floor.
Finally, immutable-tape dynamic-programming resegmentation yields exact
132-letter paths such as ``tr ad er are wa st em et ...''; lexical admission
rejects these fragments, and a clause-bigram model on a fresh exact tape is
the next test.  Each row stores its source file, construction choices, two
independent checks, novelty preflight, and repair operator; none is presented
as readable output.

The latest typed-frame repair adds the fresh 40-letter sentence ``The baker
carries a letter near the quiet harbor.''  Its eight live obligations remain
open, while every mechanical quality check other than exactness passes.  The
next run changes only the adjunct boundary lexeme selected by the first
residual obligation.  That targeted repair changes ``near'' to ``by'', yielding
``The baker carries a letter by the quiet harbor''; it remains non-exact at 38
letters, below the mechanical length floor, and records the determiner slot as
the next repair.

Two further joint searches extend the constructive evidence.  A central-pivot
CSP retains the intact 113-letter pair ``The archivist records the carefully
folded winter map at dawn. A patient sailor observes a weathered cedar rescue
boat beside the river.''  Its first mirrored characters already disagree, so
both independent audits reject it.  A semantic-slot lattice renders 108
jointly selected states and prunes all 108 before admission.  A bidirectional
decoder also reproduces the 51-letter ``Doc, note: I dissent.'' palindrome,
but the provenance gate identifies it as catalogue text and keeps it out of the
result set.

The word-pair graph contributes a 164-letter intact scene frontier but no
closure.  Paired semantic mutation produces two fresh 61-letter controls and
no closure.  Finally, weighted reverse-tape segmentation retains two authored
103--104-letter clauses but finds no legal segmentation.  These runs are
separate construction states; each leaves an explicit repair rather than being
counted as a larger duplicate sweep.

A residual repair then produces a 239-letter four-clause scene from fresh
independent clause authoring; its first boundary remains unmatched under both
audits.  A separate semantic-slot repair adds two new 108--109-letter scenes,
again with complete slots and no exact closure.  Both are retained as the next
reader-facing controls and specify a local boundary or paired-slot repair.

The character-LM grammar decoder retains a 77-letter legal-prose frontier.  A
new dependency-seam CSP adds a 105-letter typed scene, and an agreement-carrying
morphology/clitic transducer adds a 122-letter scene.  All three are fresh,
ordinary-order constructions with independent audits and explicit repairs;
none closes the global character equation.

The joint CFG/Earley intersection adds one 140-letter scene from a paired
bidirectional chart.  A 729-row cross-product variant is explicitly excluded
as a duplicate sweep, preserving its evidence without counting it as progress.
The rendered controls are ``a patient child carries the river trail at dusk.
the keeper opens old wooden gates before dawn,'' ``The patient archivist
carefully maps a quiet museum archive before dawn. Curious visitors study faded
stars beside the river,'' ``When the careful archivist restores the damaged
ledger, she records each witness name and sends the sealed copy to the harbor
office before winter,'' and ``The patient gardener waters the cedar seedlings
beside the schoolhouse before sunrise The teacher labels every seedling and
stores the tools beneath the quiet porch.''  Their independent audits all
reject exactness; each has a named local repair and remains reader-ineligible.

The hand-authored clause lattice then aligns five fresh complete clauses across
word boundaries, with an explicit all-different content-word gate.  It yields
the longest current intact-prose frontier (308 letters): ``Mara carries warm
bread to the river. The careful pilot studies cloud maps. Children gather
bright shells by moonlight. A gardener shelters young cedar shoots. Old
friends share stories beside fire. By the hearth, new tales begin. Near the
grove, small finches settle. At twilight, the patient tide returns. Beyond the
hills, a quiet engine waits. At the shore, Owen listens for bells.''  The
independent pointer and forward/reverse SHA checks disagree at the first
character, so it is explicitly non-exact and non-admitted.  Its provenance,
fresh lexical bank, and next semordnilap-compatible slot repair are recorded
in the run artifact; it is a reader-facing control, not a palindrome claim.

Three follow-up constructions add new state representations rather than larger
sweeps.  A boundary-conditioned finite-state resegmentation produces a
270-letter six-clause scene; a dependency-boundary CSP exposes 127 live
character equations in a marine-biologist scene; and a residual semantic-slot
lattice produces a 122-letter observatory scene while locking valency,
agreement, and content-word uniqueness.  Each has a first-mismatch pointer
trace, unequal forward/reverse hashes, fresh provenance, novelty preflight, and
a targeted next repair.  None is exact or mechanically admitted.

The outside-in scene grammar was then repaired to use distinct held-out right
frames instead of reflecting a left clause. Its longest pair is ``At first
light, the marine biologist records patient observations beside the sheltered
tide pool. At dusk, the coastal engineer maps hidden channels.'' (124 letters);
six fresh 102--124-letter prose pairs survive an all-different content-word
filter. An exact-tape-first control
does close on ``Able was I; I saw Elba,'' but novelty and length gates exclude
that known 16-letter catalogue sentence; a fresh 72-letter exact tape is also
excluded because its reflected half is gibberish. Finally, a ten-clause residual solver
retains a 355-letter clinic scene: ``At first light Mara opens the clinic,
checks the quiet generators, greets the two nurses, and records the medicine
count. She carries clean water to the waiting room, labels each parcel, phones
the mountain driver, updates the weather board, thanks the volunteers, and
closes the ledger before dusk. After supper she inventories the blankets,
answers the radio, repairs a torn notice, and leaves clear instructions for the
morning shift.'' All three results carry independent audits,
provenance, and novelty records; none is mechanically admitted. A finite
clause automaton adds a 109-letter archivist/visitor scene, a typed
semordnilap lane adds a 100-letter baker near miss beside the rejected short
``Stressed desserts.'' control, and a two-sided discourse equation adds a
156-letter Nora/harbor scene. These lanes retain dual audits and targeted
repairs but no mechanically admitted output.

The fresh ten-lane rerun keeps the construction dimensions separate.  The
character-LM beam emits a 227-letter archival scene; immutable-tape boundary
DP retains a 75-letter prose fallback after finding no grammatical segmentation;
dependency seam CSP, agreement morphology, CFG/Earley intersection, and a
live human-authored scene lattice emit 123-, 155-, 270-, and 117-letter scenes;
typed valency/attachment and semantic slot repair emit 100 and 170 letters;
and flat compositional grammar reaches a 208-letter action sequence without
nested palindrome spans.  Each row is rendered and independently pointer/hash
audited with provenance, novelty preflight, and a named repair.  A 126-letter
exact surface from the inflection/clitic lane is quarantined because it repeats
the catalogue clause ``A man, a plan, a canal, Panama.'' six times; it is not
counted as generated readable evidence.

The subsequent held-out inflection/clitic repair replaces that repeated unit
with a distinct 128-letter harbor scene carrying plural, past-tense, and
possessive-clitic registers.  Its pointer and hash audits still reject exact
closure, but the route demonstrates a concrete anti-shortcut repair rather
than accepting the duplicate control.

Three post-audit continuations extend the construction state without widening a
duplicate sweep: a free-center semantic machine (138 letters), an independent
phrase-level reverse parser (nine 116--118-letter probes), and a finite-domain
clause-equation SAT model (116 letters).  Their rendered clauses, independent
pointer/hash audits, provenance, novelty preflight, and named repairs are
included in the evidence ledger; none reaches the reader gate.

The newest outside-in role-phrase equation lane adds a distinct constructive
state rather than another duplicate sweep.  It expands 256 fresh role-typed
phrase pairs while checking the live outer character obligation before each
completion.  Its best intact rendering is ``The weathered cartographer examines
a fragile sextant beside the northern observatory. A patient instrument maker
restores a brass chronometer inside the maritime archive.'' (147 normalized
letters).  An independent two-pointer comparison and forward/reverse SHA
check both reject exactness; the run records its lexical choices, anti-shortcut
flags, novelty preflight, and the next repair at the first open outer role
phrase.  It is retained as a reader-facing construction probe, not presented
as a palindrome.

Three additional Luna lanes keep the state spaces orthogonal.  A bounded
character-LM obligation beam retains 64 representative realizations from
4,096 bilateral completions; its best is ``The careful archivist studies young
cedar seedlings inside the village school. A careful cartographer examines
silver seed cases across the maritime archive.'' (134 letters).  A fresh
CFG/Earley character-equation forest retains two complete parses, including
``The quiet botanist studies a folded survey beside the river station. The
careful cartographer records a weathered notebook before the evening bell.''
(124 letters).  A first-residual semantic-slot repair lane keeps six complete
seed/repair scenes, with a 108-letter beekeeper/ranger witness.  All three
lanes independently reject exactness, preserve provenance and anti-shortcut
flags, and name a held-out next repair; none is promoted to reader material.

The next continuation adds a center-free equation ledger and an inflectional
boundary repair.  The center-free lane jointly selects a semantic center after
choosing the outer clauses and retains ``The quiet engineer tests a repaired
valve beneath the river bridge. Meanwhile a small lantern warms the reading
room. A diligent teacher prepares clear lessons for the village school.'' (152
letters).  The morphology lane preserves five complete SVO/PP scenes and five
held-out suffix/clitic repairs; its best repair is ``The patient surveyor
records the northern route near the cedar bridge.'' (59 letters).  Both have
independent pointer/SHA checks, fresh provenance, novelty preflight, and
concrete next repairs; neither is exact or reader-eligible.

Finally, a dependency-tree seam CSP expands 64 attachment-compatible scene
states with distinct agents on the two complete clauses.  Its best rendering
is ``Two watchful sailors water young seedlings toward the harbor office, then
The careful archivist repairs the brass lantern beside the north window during
the long afternoon.'' (145 letters).  The dependency reparse, two-pointer
audit, and forward/reverse SHA check agree that it is non-exact; lexical
distinctness, provenance, novelty, and a held-out attachment repair are all
recorded.  It remains construction evidence rather than a readable-palindrome
claim.

A held-out dependency-role repair then changes one complete scene by replacing
the first residual role pair rather than replaying the 64-state CSP.  Its best
rendering is ``The careful courier delivers folded maps beside the weathered
shed. The watchful sailor repairs loose rigging near the harbor. The young
porter carries sealed parcels toward records offices.'' (160 letters).  The
lexicon and distinctness gates pass, while independent pointer/SHA validation
still rejects exactness; the artifact records provenance, novelty preflight,
and a concrete next repair at the second-clause attachment seam.

The following dialogue speech-act, typed morpheme-compound, and first-token
equation probes add 130-, 68-, and 91-letter ordinary-language renderings.
They remain diagnostic controls because the first two do not close and the
morpheme realization also fails the lexicon gate.

An A* typed word-boundary probe retains a 140-letter garden/schoolhouse scene,
while a single-slot human semantic search retains seven deduplicated
92--97-letter scenes.  An attested phrase-bridge preflight found no complete
clause and is excluded.  These outcomes are recorded as bounded construction
evidence, not as larger duplicate sweeps.

Three further Luna continuations add distinct constructive states: a paired
lexical grammar renders six fresh 75--82-letter SVO pairs, a free-center
semantic bridge renders a 109-letter scene, and a productive grammar-pair
composition grows complete paired productions through 55, 114, 168, and
223-letter states.  Each has independent pointer/SHA validation, provenance,
novelty preflight, and a first-residual repair; all remain non-exact and are
retained as bounded evidence rather than duplicate sweeps.

A typed onset/rime grapheme constructor contributes 12 corrected 37--47-letter
complete clauses, and a reader-first function/inflection editor contributes
five 96--104-letter scenes.  Both remain non-exact after independent audits.
An entailment-preserving active/passive rewrite is retained only as excluded
failure evidence because novelty preflight found overlap with existing voice
and information-structure families.

Three additional Luna lanes keep the construction state distinct rather than
resampling a prior sweep.  A constrained edit program renders four intact
97--106-letter versions of one cartographer/ranger scene while lowering
mirrored-character debt from 51 to 44; an append-algebra probe renders four
complete 28--102-letter clause-growth states and records the suffix-obligation
invariant; and a discourse-relation involution renders 12 complete 43--56-letter
cause/effect or contrast propositions.  Each has fresh provenance, a novelty
preflight, independent pointer/SHA replay, and a named next repair.  None is
exact or reader-eligible.

The next repair wave adds three more orthogonal states.  A typed semantic
center-out solver renders 12 complete 95--109-letter scenes; a lexical
word-equation intersection renders four complete 133--143-letter scenes; and
a joint slot/boundary repair renders nine complete 45--62-letter scenes.  The
representative surfaces are ``The patient curator shelters the fragile maps
during the storm; the careful teacher copies the final field notes beside the
window.'', ``The curator labels the fragile map before the archivist stores the
ledger, while rain gathers softly against the western windows and visitors wait
beside the reading room.'', and ``The quiet curator labels the old faded map
before dawn.''  Their normalized tapes are independently rejected by
two-pointer and forward/reverse SHA checks; each run records provenance,
 novelty, and a concrete boundary repair.

Two further Luna lanes keep the search constructive. A character-level beam
over typed semantic frames emits ``The young botanist studies the silver seed
cases beside the greenhouse; a careful pilot marks the distant landing lights
through the mist.'' (115 letters; first mismatch 1; normalized SHA prefixes
``e57cebaf...'' and ``99cbaec3...''). A fresh CFG/Earley character
intersection emits ``At first light, the surveyor records the river current
while the baker warms bread for the waiting crew, and the harbor keeper checks
the lamps before opening the gate.'' (137 letters; first mismatch 0; SHA
prefixes ``a24dca02...'' and ``8b577593...''). Both are complete prose with independent
pointer/SHA checks, provenance, novelty preflight, and a typed next repair;
neither is exact or reader-eligible.

Three further Luna constructions are retained as fresh states. A paired
semantic CFG produces a 108-letter two-clause scene; a joint semantic/
inflection word-boundary DP produces a 109-letter scene with independent
sentence boundaries; and an online clause-order lattice produces a 151-letter
four-sentence archive scene. Their rendered surfaces, pointer/SHA audits,
provenance, novelty checks, and next repairs are recorded in the evidence
ledger. None is exact or reader-eligible.

The newest bounded continuations change the state representation again. An
endpoint-aware bilateral seam reserves lexical terminals before interior
character decoding (six 100--105-letter scenes); a fresh-scene tape/CFG
intersection keeps boundaries mutable and records a 126-letter lexical repair;
and a typed semantic-slot neighborhood changes one role-compatible slot in six
109--160-letter scenes. All retain intact prose, independent two-pointer and
forward/reverse SHA audits, provenance, novelty preflight, and a concrete next
repair. None closes exactly, so none is promoted to the reader packet.

The latest constructive continuations add a nested-free slot-boundary DP, a
held-out semantic phrase-edge joiner, a coupled object/attachment repair, a
role-typed reversible-clause product, and a POS-filtered reversible-clause
composer with appendable frame growth.  They produce intact 98--124-letter
scenes (including a 111-letter harbor/reader clause), keep character equations
live during construction, and record independent audits, provenance, novelty,
and the next repair.  They add construction states rather than duplicate
sweeps; none has yet closed the exact tape.

Two additional fresh states extend this evidence without changing the gate.  A
seed-free authored-frame insertion lane renders the 83-letter scene ``At dawn,
the archivist opens the cedar cabinet and records the harbor map, before dusk.''
An interrogative/relative-template solver renders the 87-letter scene ``Was the
quiet curator sure that the young pilot had seen the chart I filed beside the
harbor ledger?''  Both are complete prose with online character obligations,
independent pointer/hash replays, provenance, novelty preflight, and a named
repair; neither is exact or reader-eligible.

The newest Luna queue adds three more non-overlapping constructive states.  A
live mirrored-tape terminal decoder emits six complete 88--103-letter scenes;
a human-authored attachment CSP emits two 105--107-letter museum scenes; and
an unbounded bilateral semantic-growth grammar emits three non-repeating
clause-growth states up to 140 letters.  Each row is independently checked,
provenance-backed, and paired with a concrete first-residual repair.  None is
an exact, mechanically admissible palindrome, so none is represented as a
reader result.

The following queue pass adds three boundary-aware states: an independently
resegmented reflected-tape grammar (two 103--107-letter scenes), a semantic
scene equation lattice with asymmetric token counts (two 111--112-letter
scenes), and a finite-state semantic boundary-macro grammar (three growth
states up to 74 letters).  These are fresh complete prose rows with
independent pointer/hash replay, provenance, novelty preflight, and concrete
residual repairs; none closes exactly or enters the reader packet.

The reset wave then changes the construction state rather than expanding a
prior sweep. A live CFG/Earley character chart renders six fresh scenes up to
102 letters; a held-out cross-word seam CSP renders sixteen 62--75-letter
role-typed scenes without a fixed tape; and POS-shaped seam mining feeds fresh
authoring of two connected 100--101-letter valency scenes. Each row is actual
prose with independent pointer/SHA validation, provenance, novelty preflight,
anti-shortcut checks, and a concrete next repair. None closes exactly.

For audit transparency, the corrected grapheme lane's representative rendering
is ``The baker repairs the gate; the pilot maps a gate.'' (39 letters, first
mismatch at 0), while the longest function-edit rendering is ``At dusk, Mara
carries the blue lantern across the quiet bridge, and Jonah records each
rescued name so the town archive can open.'' (104 letters, first mismatch at
0).  Their normalized forward/reverse digests disagree; neither is presented
as a readable palindrome.

The latest three Luna continuations keep the gate unchanged while changing the
construction state. Held-out reported-speech frames retain four vivid
diagnostics up to 121 letters; asynchronous aspect transitions retain 288
complete controls up to 164 letters; and nested adjunct ordering retains 36
paired controls up to 161 letters. All three have zero exact candidates above
38, and their rendered rows remain diagnostic controls rather than reader
claims. Their immutable artifacts record the next construction as
narrative-voice frames, explicit residual handoffs, and bounded third-adjunct
scope, respectively.

The topology reset then changes the construction state rather than extending
those seams. An author-first semantic lattice explores 27 live scene states up
to 53 letters; an independent shared-scene clause matcher retains 20
non-repeated controls up to 53 letters; and a factorized full-clause CSP visits
5,000 states but reaches no complete two-sided parse (its longest control is
108 letters). All three have zero exact candidates above 38. They remain
diagnostic construction traces, with the next states recorded as multi-character
residuals, a second typed event, and held-out valency frames.

The next queue pass widens those states without editing emitted strings. The
number-aware semantic lattice carries a two-character residual through
singular/plural scene frames (36 states, 58-letter diagnostics); the
valency-alternation CSP adds held-out ditransitive/intransitive agreement but
reaches no complete parse in 5,000 bounded states; and the two-event scene
matcher retains 20 non-repeated controls up to 53 letters. All exact counts
above 38 remain zero, so these traces stay diagnostic-only while their next
construction topologies are preserved in the run artifacts.

Subsequent continuations keep the same constructive gate while widening the
state product: benefactive/locative attachment exhausts 5,000 bounded states;
asynchronous width-two semantic slots retain 58-letter diagnostics; and
event-order/tense matching retains 20 complete controls up to 65 letters. No
lane has an exact candidate above 38, so none is sent to readers; each records
the next live construction rather than editing a failed surface.

\section{Independent verification and provenance}
For every row, the verifier recomputes normalization, length, first mismatch,
two-pointer equality, and a forward/reverse digest without importing the
generator's booleans.  The mechanical admission vector records ASCII support,
lexical form, distinctness, catalogue-family exclusion, repeated-unit and
self-palindrome exclusion, and non-word-order symmetry.  The registry assigns
one signature to each run and rejects duplicate families before aggregation.
On the authoritative snapshot the registry contains 431 unique artifacts (35
preflight exclusions); the aggregate contains 5,529 rows, 84 exact rows, and
zero mechanically admitted rows across 332 route summaries.  Every exact row
therefore has a visible reason for rejection or promotion status rather than
being silently discarded.  The aggregate report is registered separately as
an audit artifact, never as a construction family.

The latest bounded wave adds six non-duplicate constructive operators.  A
finite-state boundary beam renders 12 intact 102--107-letter scenes; an
agreement-carrying morphology transducer generates 24 retained 85--88-letter
clause pairs from 144 complete clauses; a human-authored scene lattice emits
three 73--83-letter scenes; a memoized POS/valency intersection emits 12
50--63-letter scenes; a semantic phrase-chunk decoder emits 15 intact
106--114-letter two-clause scenes; and a held-out paired-slot repair emits
three independently exact 101-letter tapes.  The longest readable rendering
is ``The careful archivist stores weathered maps beside the north window. A
quiet teacher reviews marked field notes near the harbor office.''  Its
independent pointer and forward/reverse SHA checks agree on \emph{not exact}.
The exact 101-letter tapes are also visible in the run artifact, but their
surface is not readable and they are rejected before any reader claim.  Each
failure names a next operation (held-out adjunct, inflectional, role-clause,
chunk, or semantic-slot repair); none is counted as progress from a duplicate
sweep.

A follow-up typed scene realizer adds three complete 39--42-letter SVO/PP
scenes by choosing an open character skeleton before lexical realization; its
best surface is ``The harbor guides the vessel near the breakwater.'' (41
letters, exact \emph{false}) and the recorded next repair reselects the object
at the first residual while preserving valency and agreement. Two other exact
controls are deliberately excluded: a 50-letter CFG chart witness is rejected
for boundary word-order mirroring and a self-palindromic span, and a 42-letter
lexical-closure witness is rejected for mirrored word order and self-collision.
Their exact tapes remain auditable failure evidence, never readability claims.

Three targeted repairs continue the same constructive discipline: a typed
boundary lattice produces a 72--75-letter night-porter scene, the manual
clause-plan inventory produces a 71-letter baker/carpenter scene, and the
repaired CFG frame produces a 124-letter mason/botanist/curator scene. Their
independent exact audits all return \emph{false}; each has fresh provenance,
novelty preflight, and a first-residual repair operator. The best intact
rendering is ``The baker bakes fresh bread before dawn; The carpenter repairs
blue sails after lunch.'' It is preserved as a readable control, not presented
as a palindrome.

The next frontier keeps the same clause-first discipline while changing the
obligation mechanism: a cross-word character trie retains eight 79-letter
SVO/PP scenes, a fresh-center composition retains twelve complete scenes, and
an LM-ranked legal-transition decoder retains twelve more. The cross-word
representative is ``The gardener carries the bell beside the garden. The
writer watches the seed within the station.'' (79 letters; exact
\emph{false}); its held-out repair replaces the first right-clause place with
``orchard''. None embeds the known seed or uses a mirrored word-order unit.

Three further orthogonal probes make the scalability claim concrete.  The
closed-form grammar family appends a fresh typed clause to a complete state,
rendering a 122-letter base, ``At dawn, the archivist catalogs river maps
beside the old observatory. Before noon, a patient courier delivers sealed
parcels to the village clinic.'', and a 184-letter one-clause extension that
adds ``By evening, the groundskeeper labels young seedlings near the rain
barrel.''  Both are ordinary, independently pointer/SHA-audited prose with
unresolved character debt; the next repair replaces the first unresolved
boundary with a fresh typed clause.  A semantic valency path solver retains
three base realizations plus three held-out role repairs; its repaired 79-letter
scene is ``The young botanist points out a coastal inlet near the market, and
keeps a quiet secret.''  All four local edge obligations close, but the full
tape remains non-palindromic; the next repair expands the consequence predicate
for whole-tape closure.
Finally, a reverse-automaton phrase lattice
produces a 51-letter exact tape, ``Doc, note: I dissent. A fast never prevents
a fatness. I diet on cod.'', but the independent gate rejects it for local
catalogue collision and a self-palindromic span.  This exact control is kept
as rejection evidence, not counted as a readable output.

The exact-candidate repair lane also tested the 50-letter witness ``A dog was
stressed; live on time; emit no evil; desserts saw God a.''  Its tape and
hashes agree exactly, but the independent gate rejects it for mirrored
word-order and a proper self-palindromic span; a semantic substitution loses
exactness.  We retain that failed repair as a concrete boundary, not as a
readability claim.

As a family switch, the semantic lane then built a distinct causal path with
initiates-cause, transforms, situated-within, and causes edges.  Its best
intact rendering is ``The marine expert points out a small fort by the coast,
and causes a quiet shift at sunset.''  All four local terminal obligations
close, but its full tape remains non-palindromic; the next repair couples the
causal consequence and locative slots.

The subsequent frontier adds three non-duplicate constructive operators.  A
lexicalized reverse-trie grammar retains five 110--123-letter clause pairs
after a held-out locative repair;
the representative is ``The careful archivist studies weathered maps beside
the quiet harbor. The thoughtful teacher records clear field notes inside the
village school.'' (123 letters, exact \emph{false}), with a held-out locative
edge repair.  A scene-first agreement/tense/clitic transducer retains 24
displayed variants from 72 jointly realized states; its longest grammatical
rendering is ``The careful gardeners watered the red roses behind the stone
wall; the notes of the careful gardeners remain ready.'' (95 letters, exact
\emph{false}).  Its next operation is a held-out possessive/clitic seam
substitution that preserves agreement.  Finally, recursive balanced grammar
emits six complete states through depth two, including the 240-letter scene
``The village doctor examines a careful sketch inside the quiet clinic. The
careful archivist stores weathered maps beside the harbor window. Meanwhile, a
patient teacher reviews marked field notes near the village school. At dusk, a
gentle keeper mends the garden gate after the winter storm.'' (exact
\emph{false}); its first-residual repair replaces the outer seam with a
role-compatible clause pair.  None is reader-eligible until exact admission.

Two additional seams are kept joint rather than repaired post hoc.  A
punctuation-center bridge retains 144 baseline/subject/object/locative/verb/agreement/determiner/adjective/noun/modifier-repaired 112--140-letter scenes,
including ``The patient cartographer marks a coastal inlet beside the old
pier; the harbor bell sounds, while the young archivist files weathered maps
near the harbor office.'' (exact \emph{false}); its next repair holds out a
locative while preserving the repaired object seam.
An affix/clitic crossword retains 49 complete 117--136-letter baseline/repaired clause pairs, including ``The seasoned harbor
pilot guides the weathered ferry through the morning fog. The kind librarian
returns the borrowed novels to the neighborhood shelves.'' (129 letters, exact
\emph{false}), with held-out possessive owner-number substitutions next.
Both lanes independently audit the full normalized tape and preserve
provenance; neither is reader-eligible until exact admission.

The dependency-semantic seam CSP v2 then searches argument attachment and
lexical domains jointly across 186{,}624 pair states.  Its three base and six
held-out repaired probes are complete ordinary clauses; the clearest is ``The archivist
studies the weathered maps within the quiet station. The messenger delivers
the marked journals within the quiet station.'' (exact \emph{false}).  The
first-residual repair holds that seam fixed and substitutes a held-out
role-compatible object/place realization before recomputing attachment.
Repeated station context is explicitly visible in its mechanical rejection
fields; this is failure evidence, not a readability certificate.

The next Dream-RSI repair carries model-proposed semantic roles through the
character search itself.  A local model produced 34 valid fresh phrase units
(one subject, eleven verb phrases, eleven objects, and eleven adjuncts), and
the decoder rejected 189{,}481 states whose two displayed halves could no
longer realize a live clause-prefix/suffix chart.  It produced no exact
closure.  This is a construction diagnostic: the sparse Brown-derived shape
inventory is the bottleneck, so the next branch replaces it with a typed
hand-authored subject/verb/object automaton while preserving the role labels
at both mirrored boundaries.  No row is sent to readers from this lane.

\section{Reader-facing evaluation}
No candidate is called readable by the automatic diagnostics.  The release
package is gated until a mechanically admitted survivor exists.  The planned
study uses intact prose and controls within five normalized letters,
randomized blinded order, opaque item identifiers with a separate key, and a
reproducible rater package.  The builder requires structured provenance with
immutable generator/source hashes and explicit false flags for copied or
borrowed text and shortcut symmetries; it also regenerates controls until their
normalized tapes are changed and non-palindromic.  Readers rate Englishness,
coherence, and grammaticality
independently; two blinded coders adjudicate free-form paraphrases.  The
analysis is preregistered before unblinding, with the candidate admitted only
if it beats the controls on the prespecified reader criteria; agreement will
be reported with a preregistered reliability statistic such as Krippendorff's
alpha \citep{krippendorff2011computing}.  The current
manuscript reports no human responses and makes no readability claim.

\section{Limitations and next construction}
The present snapshot has not yet produced a $\geq100$-letter candidate that
passes all gates.  That is a development status, not the intended contribution.
The next run will change the construction state rather than repeat a sweep:
the seam lane will use typed boundary resegmentation and then a fresh
non-palindromic subject/verb/object frame while preserving agreement; the scene
lanes will solve an object/attachment slot against the live tape; and the LM
lane will propose new clause boundaries on held-out vocabulary.
Each resulting surface will be rendered and audited before a reader-facing
test.  Once a survivor appears, the reader package and paper tables will be
regenerated from its immutable provenance record.

\bibliography{refs,refs-dreamrsi}

\appendix
\section{Reproduction checklist}
Run the ten lane drivers, then the candidate-readability audit and novelty
validator scripts in \texttt{tools/} and \texttt{experiments/}.  The former
recomputes exactness and mechanical gates; the latter checks unique signatures
and required repair artifacts.  The committed JSON files under \texttt{runs/}
contain the rendered text, normalized lengths, hashes, provenance, and next
repair for every row.  A reader package is generated only after the mechanical
admission count becomes nonzero.
\end{document}
