# A* typed word-boundary search (2026-09-16)

A fresh A* lane expanded typed lexical chunks and the opposite-side word boundary jointly. Grammar and semantic valency were hard constraints. It imported no catalogue sentence, used no fixed tape, repeated spans, mirrored word order, or seed wrapper.

The retained output is intact ordinary prose and exceeds 100 letters, but does not close exactly. The run records direct normalized-tape, two-pointer, and independent SHA-256 forward/reverse audits, plus generator hash and novelty preflight. The concrete repair is mismatch-directed replacement of a typed chunk pair while preserving argument structure, followed by reopening both boundary frontiers.
