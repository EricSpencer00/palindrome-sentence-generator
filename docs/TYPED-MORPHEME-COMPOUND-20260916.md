# Typed morpheme/compound boundary construction (2026-09-16)

This run jointly realizes ordinary clauses using fresh prefix+stem+suffix
forms (`repainter`, `repaintable`, `unreadable`, etc.) and explicit compounds
(`work+shop`, `rain+coat`). The morpheme boundaries are typed as agent,
progressive, or adjective; no whole-word semordnilap, catalogue seed,
repeated unit, or word-order mirror is used.

The 54 authored probes were audited with independent two-pointer comparison,
forward/reverse SHA-256 tape hashes, and mechanical admission checks. No probe
reached the requested 100 letters or exact closure. The longest readable
near-miss is recorded in the run artifact, with its first mismatch and hashes.
Its repair is concrete: replace only the right prefix+stem+suffix adjective at
the first failing character while preserving the typed mural semantic role.

See [run JSON](../runs/typed-morpheme-compound-20260916.json).
