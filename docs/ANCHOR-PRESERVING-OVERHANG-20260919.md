# Anchor-preserving overhang extension — 2026-09-19

The center-out overhang search extended the 38-letter readable anchor to a
132-letter exact tape:

> *Name not left onto her a. Set add new one last one. Can all its an aide
> rips nine memos some men inspire Diana still an. Ace not sale now end dates.
> Are hot not felt one man.*

The normalized tape passes an independent two-pointer comparison and matching
forward/reverse SHA-256 audits. It is longer than the 24-letter Panama control
and the 38-letter anchor.

It is not a new accepted readable palindrome. The output contains the complete
38-letter anchor as a proper self-palindromic span and repeats a content word,
so the shared mechanical admission gate rejects it. The result is retained as
an overhang-search frontier, not as public prose.

The full machine-readable record is
`artifacts/anchor-preserving-overhang-20260919.json`. No local model inference
or heavy benchmark is required to verify it.
