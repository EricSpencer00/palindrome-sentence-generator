# Hand-authored function/edit extension

This diagnostic authors a coherent two-clause rescue scene first, then tries a
small held-out set of ordinary determiner, tense, subject, and purpose edits.
Each complete sentence is rendered before character equations are inspected.
The run imports no 38-letter seed, catalogue prose, word-order mirror, or
semordnilap chain. Independent pointer and normalized SHA audits are recorded
for every state. The 2026-09-16 run produced five readable states and no exact
closure; the next repair changes the exposed verb-object boundary jointly.
