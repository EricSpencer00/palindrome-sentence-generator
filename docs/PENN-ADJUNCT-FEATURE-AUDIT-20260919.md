# Adjunct feature-partition audit

The `322ce9a5` expansion is retained as a diagnostic, not as a new
feature-conditioned result. It extracted preposition--noun pairs, but then
copied the same global adjunct inventory into each singular, plural, and past
feature bank; it did not condition an adjunct on the feature of the clause in
which it occurred. The run also retained the existing two templates rather
than adding a separate prepositional-object derivation. No candidate is
promoted. A valid follow-up must extract adjuncts from the same tagged frame
and carry that frame identity into the slot state before character search.
