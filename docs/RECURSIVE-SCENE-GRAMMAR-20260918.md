# Recursive semantic-scene grammar (2026-09-18)

This lane generated semantic scene trees independently on the two sides of the
candidate and tested the complete normalized character equation. It did not
reverse a finished string or copy catalogue text.

The depth-one grammar produced 729 derivations per side and 531,441 pair
probes. None was equation-compatible, so there are 0 exact closures and no
reader-eligible output. The saved controls are complete scene realizations with
independent two-pointer and SHA-256 audits.

The next constructive repair is to carry the mirrored residual into recursive
valency choices before expanding a child scene.
