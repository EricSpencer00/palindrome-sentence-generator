# Paraphrase-graph debt paths (2026-09-16)

## Novelty preflight

The registry was inspected before execution. No prior signature matched
`complete-authored-scene-graph|meaning-preserving-paraphrase-edges|two-normal-order-paths|mirrored-character-debt|heldout-edge-repair|independent-four-audit` (the packaged row is ignored during replay). The method is distinct from scene lattices and global semantic mutation: the search state is a directed walk through complete authored realizations, and each edge is an explicit meaning-preserving paraphrase.

## Method and evidence

Nine left paths and nine right paths produced 81 intact probes, all 117–408 letters. The solver keeps ordinary word order on both paths and reports the mirrored character debt after consuming the longest matching prefix of the left tape against the reversed right tape. Every node is authored prose; no corpus sentence, catalogue palindrome, wrapping, or word-order reversal is used.

Representative base prose:

> At dawn, the harbor pilot checks the warning lamps and marks the tide. At dusk, the station keeper locks the gate and tells the waiting travelers.

Held-out edge repair (`r2→r3`) produced:

> Before breakfast, the harbor pilot tests the harbor lights and writes down the tide. After sunset, the station keeper closes the gate and informs the patient travelers.

Independent exact, two-pointer, SHA-256 reverse-tape, and mechanical readability checks agreed for every probe and repair. Results: 0 exact closures, 0 mechanically admitted probes; the repair remained readable (138 letters) but was not a palindrome. Evidence is frozen in `runs/paraphrase-graph-debt-paths-20260916.json`.
