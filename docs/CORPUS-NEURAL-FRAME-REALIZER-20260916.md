# Corpus frame realizer (2026-09-16)

The novelty preflight found no exact signature collision in the registry. This
route plans an agent–action–artifact semantic frame from the authored corpus,
realizes each side independently, and uses a character equation only to
constrain lexical choices. A corpus bigram model orders proposals after
constraint propagation; no neural model is used and it is not a readability
judge.

Hard gates reject repeated content words, normalized catalogue tapes, and any
non-exact tape. No clause-bank, reverse-prefix, center-out, beam, MCTS, or
grammar-intersection machinery is used. The frozen run contains rendered
probes, exact audits, corpus/catalogue hashes, and provenance. It produced no
mechanically admitted or reader-eligible output. No readability claim is made
because blinded readers were not run.

Replay with:

```text
python3 experiments/corpus_neural_frame_realizer_20260916.py
```
