# Phrasewise equation rejection at the 568 clean seam

This is a 578-letter exact child of the pinned 568 parent, preserved as an
audit control and rejected as a shortcut. Its complete rendering,
independent validation, normalized replacement spans, and provenance are in
[`runs/incumbent-568-phrasewise-equation-audit-20260923.json`](../runs/incumbent-568-phrasewise-equation-audit-20260923.json).

At the clean mirrored seam `[148,163]` / `[405,420]`, the inserted clauses
were:

> Reda Mar diapered Elena. / Ane Lede repaid Rama Der.

Their 20-letter tapes are exact reverses, and replacing the two 15-letter
parent spans produces a 578-letter palindrome. The full normalized tape has
SHA-256 `e196e42e08e3ed43ac39e84092730c6d28f2eb22699d83331f36d3927d47fb7c`.
The candidate was generated by bounded clause substitution; no completed
tape was reversed to create it. It has no human readability evidence, and
the proper-name cluster is not presented as readable prose.

Exactness does not make the local construction acceptable. The left clause
has internal letter boundaries `{4, 7, 15}`. Reflecting the right clause's
boundaries gives `{3, 7, 13, 17}`. Their shared offset `7` factors the equation:

```text
redamar      = reverse(ramader)
diaperedelena = reverse(anelederepaid)
```

Thus the pair is two word-aligned reverse phrase units hidden inside a
token-asymmetric equation. The new `phrasewise_reverse_boundary_offsets`
check rejects that local structure. As a positive control, splitting the
38-letter seed at its central word gap—“An aide rips nine memos” / “some men
inspire Diana”—produces no shared reflected internal word boundary.

**Disposition:** audit-only exact control, not a candidate or a win. The
`diapered` / `repaid` template is abandoned; name substitutions preserve the
same subject/object decomposition. The next bounded construction preregisters
different boundary masks for an auxiliary-plus-participle clause and a
finite-verb-plus-object-complement clause before choosing words. No reader
test is appropriate for this rejected, name-heavy example.
