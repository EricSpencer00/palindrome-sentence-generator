# Clause-pair CSP evidence

Run `python experiments/clause_pair_csp_20260916.py --min-letters 100`.

The run intentionally retains the longest intact two-clause prose when no
exact closure is found, including the complete unmatched character debt.
