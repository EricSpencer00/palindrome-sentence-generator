# Two-character outer-class CSP (2026-09-21)

This lane is a bounded construction diagnostic, not a readable palindrome.
It chooses a two-letter prefix class and requires the final two letters of the
tail, read in reverse, to equal that class before rendering the interior
slots.  That is an actual outside-in condition; comparing the suffix in its
forward orientation would be incorrect.

The run evaluated 243 outer assignments, pruned 162 before interior rendering,
and retained 81 complete, lexically disjoint prose controls.  The longest
control is 82 normalized letters.  Every retained control misses the center
seam, and independent pointer plus SHA-256 forward/reverse audits found zero
exact candidates above 38 letters.  No control is reader-certified.

The next construction is therefore specific: retain the matched two-character
class and author a new tail whose interior boundary also satisfies a two-letter
residual, rather than widening this inventory or treating length as progress.
