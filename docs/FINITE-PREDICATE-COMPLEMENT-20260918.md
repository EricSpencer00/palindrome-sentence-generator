# Finite predicate complement repair (2026-09-18)

The 44-letter discourse diagnostic had a fragmentary center. This bounded
repair replaced that center with independently authored complete finite
predicates: each has an explicit subject, finite verb, and object. Surrounding
clauses and predicate choices came from separate small banks; no word or tape
was reversed to make a candidate.

## Result

The run checked 200 authored combinations (5 left clauses, 8 finite
predicates, and 5 right clauses, bounded before the final 200th world). It
found 0 exact palindromes, 0 mechanically admitted candidates, and 0
reader-eligible outputs. The first rendered control was:

> Was Noel an era, the poet reads a poem; a gas.

Each stored row has the rendered text, clause/predicate provenance, a
two-pointer normalized-letter audit, forward/reverse SHA-256 values, and all
mechanical gate results. Human readability was not inferred from these
programmatic results.

## Next repair

The center must be solved token-by-token against residual outer characters,
while retaining subject–verb agreement and held-out scene roles. Replacing a
fragment with a grammatical predicate alone is insufficient when the
character equation is deferred until after composition.
